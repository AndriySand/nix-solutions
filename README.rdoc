== README

This README would normally document whatever steps are necessary to get the
application up and running.

You need create DB, so in console make
rake db:create
rake db:migrate

Then run rake task with command
rake parse_html